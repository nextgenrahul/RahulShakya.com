version: "3.9"

networks:
  rahul-network:
    driver: bridge

volumes:
  postgres-data:
  pgadmin-data:

services:
  postgres:
    image: postgres:16-alpine
    container_name: rahulshakya-postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: rahulshakya
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123

    ports:
      - "5433:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d rahulshakya"]
      interval: 10s
      timeout: 5s
      retries: 5

    networks:
      - rahul-network

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: rahulshakya-pgadmin
    restart: unless-stopped

    environment:
      PGADMIN_DEFAULT_EMAIL: admin@gmail.com
      PGADMIN_DEFAULT_PASSWORD: admin123

    ports:
      - "5051:80"

    volumes:
      - pgadmin-data:/var/lib/pgadmin

    depends_on:
      postgres:
        condition: service_healthy

    networks:
      - rahul-network

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: rahulshakya-api
    restart: unless-stopped

    env_file:
      - ./backend/.env

    ports:
      - "8000:8000"

    volumes:
      - ./backend:/app

    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload

    depends_on:
      postgres:
        condition: service_healthy

    networks:
      - rahul-network

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile

    container_name: rahulshakya-web
    restart: unless-stopped

    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000

    ports:
      - "3000:3000"

    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next

    depends_on:
      - api

    networks:
      - rahul-network